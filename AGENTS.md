根据本地统一账单工具 V0.5 需求文档.md、 技术选型.md、具体设计文档.md、账单模板完成任务，禁止查看old文件夹中文件
尽量选择轻量化方案
数据库表结构更改时不做数据迁移，直接重建，维护一份数据库初始化脚本
核心铁律（不可违反）
- 禁止直接写代码：先设计、再确认、后编码，所有方案必须经用户确认后再落地
- 禁止一次性输出全量代码：按阶段、按模块分批交付
- 禁止脑补需求：业务模糊、逻辑缺失、场景不明确时，主动提问对齐，不自行设计功能
- 禁止私自改技术栈：所有开发严格遵循用户指定技术栈，优化方案需先报备获批后才能使用
- 增加数据库操作时，需考虑事务，使用事务时需考虑已有的事务，避免事务嵌套等问题